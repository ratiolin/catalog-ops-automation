# 商品上架自动化真实回放证据

- 运行 ID：`fa2b4f0a-6460-4658-a8e1-af73970f963c`
- 证据日期：2026-07-02
- 场景边界：合成商品、作品集沙箱，不代表真实业务收益。

## 最终状态

| 环节 | 数量 |
|---|---:|
| CSV 输入 | 30 |
| 确定性校验失败 | 10 |
| Dify 生成候选文案 | 20 |
| 当前已写入 Odoo | 20 |
| 当前 ERP 写入错误 | 0 |
| Odoo 演示 SKU 数 | 20 |

真实链路为：模拟 CSV → 确定性异常校验 → 已发布 Dify 工作流 → 影刀 6.0.30 编排 → Odoo XML-RPC 幂等写入 → Metabase 看板。

## 审计解释

`erp_operations` 中保留了 20 次 `written` 和 23 次 `failed` 操作尝试。23 次失败来自页面定位及 RPC 兼容调试阶段，没有被删除或改写；最终当前商品状态是 20 条 `erp_written`、0 条当前写入错误。看板卡片 40 展示当前状态，卡片 43 展示包含历史失败的操作尝试，两者不可混为一谈。

## 已验证控制

- 只有 `approved.csv` 能进入影刀，10 条异常商品不会越过门禁。
- Odoo 写入前按 SKU 查询模板和变体，重跑不创建重复商品。
- 每条回调使用稳定 `operation_key`，服务端唯一约束保证幂等。
- ShadowBot `run` 不使用 Python 默认参数，规避 6.0.30 的空参数名问题。
- Odoo 字段通过 `fields_get` 动态发现，产品类型和可选字段可降级。
- Metabase 业务数据源使用只读账号；当前状态和历史尝试分开展示。

机器可读版本见 `run-fa2b4f0a.json`。
